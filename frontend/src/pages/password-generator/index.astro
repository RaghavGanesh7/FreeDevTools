---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PasswordGenerator from './_PasswordGenerator.tsx';
import { getToolByKey } from '../../config/tools';

const tool = getToolByKey('password-generator');
---

<BaseLayout 
  title={`${tool.name} - TODO: Add subtitle | Free DevTools`}
  description={tool.description}
  canonical={tool.canonical}
  themeColor={tool.themeColor}
>
  <PasswordGenerator client:load />
</BaseLayout>
