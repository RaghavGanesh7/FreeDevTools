---
import type { Tool } from '@/config/tools';
import { getToolByKey } from '@/config/tools';
import BaseLayout from '@/layouts/BaseLayout.astro';
import AnthropicTokenCounter from './_AnthropicTokenCounter';
const tool = getToolByKey('anthropic-token-counter') as Tool;

---

<BaseLayout 
  name={tool.name}
  title={tool.title}
  path={tool.path}
  description={tool.description}
  canonical={tool.canonical}
  
  keywords={tool.keywords}
  ogImage={tool.ogImage}
  twitterImage={tool.twitterImage}
  datePublished={tool.datePublished}
  softwareVersion={tool.softwareVersion}
  features={tool.features}
>
  <AnthropicTokenCounter client:load />
</BaseLayout>
